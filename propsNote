// props are one of the core concepts in React. They let you pass data from a parent component down to a child component.

// Let me show you different instances where props are used 👇

// 1. Passing simple props
// const Product = (props) => {
//   return (
//     <div>
//       <h2>{props.name}</h2>
//       <p>Price: ${props.price}</p>
//     </div>
//   );
// };

// const App = () => {
//   return (
//     <div>
//       <Product name="Laptop" price={1200} />
//       <Product name="Phone" price={800} />
//     </div>
//   );
// };


// 👉 Here, name and price are props passed into the Product component.

// 2. Destructuring props

// Instead of writing props.name every time, you can destructure:

// const Product = ({ name, price }) => {
//   return (
//     <div>
//       <h2>{name}</h2>
//       <p>Price: ${price}</p>
//     </div>
//   );
// };

// 3. Passing objects/arrays as props
// const Product = ({ details }) => {
//   return (
//     <ul>
//       <li>{details.name}</li>
//       <li>${details.price}</li>
//       <li>{details.availability}</li>
//     </ul>
//   );
// };

// const App = () => {
//   const product = { name: "Tablet", price: 600, availability: "In stock" };
//   return <Product details={product} />;
// };


// 👉 Instead of passing multiple props, we pass the whole object as one prop.

// 4. Passing functions as props

// Props can also be functions, useful for events:

// const Button = ({ handleClick }) => {
//   return <button onClick={handleClick}>Click Me</button>;
// };

// const App = () => {
//   const sayHello = () => alert("Hello from props!");
//   return <Button handleClick={sayHello} />;
// };

// 5. Children prop

// Anything between a component’s opening and closing tags becomes props.children:

// const Card = ({ children }) => {
//   return <div className="card">{children}</div>;
// };

// const App = () => {
//   return (
//     <Card>
//       <h1>This is inside the Card</h1>
//       <p>Passed as props.children</p>
//     </Card>
//   );
// };

// 6. Default props

// You can define default values if a prop isn’t provided:

// const Product = ({ name, price = 100 }) => {
//   return (
//     <div>
//       <h2>{name}</h2>
//       <p>Price: ${price}</p>
//     </div>
//   );
// };

// const App = () => <Product name="Book" />; // price defaults to 100


// ⚡ Summary of where props are used:

// To pass data (name, price)

// To pass objects/arrays

// To pass functions (event handling)

// To render nested components (props.children)

// To set defaults for missing props